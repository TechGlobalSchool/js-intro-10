// Create a function that takes 2 numbers as arguments and return their average

// average1
// average2

function average1(a, b) {
  return (a + b) / 2;
}

const average2 = function(a, b) {
  return (a + b) / 2;
}

// Arrow function
const average3 = (a, b) => (a + b) / 2;
