let str = '    Tech        Global       ';

console.log(str.trim()); 
console.log(str.trimStart()); 
console.log(str.trimEnd()); 

let greeting = 'Hello';

console.log(greeting.trim());

console.log(str);

/*
trim()
TASK: removes the whitespaces at the beginning and at the end of the string
RETURN: returns a new modified string
ARGUMENTS: No args
CHANGE ORIGINAL VALUE: does not modify the original value
STATIC or NON-STATIC: non-static
*/